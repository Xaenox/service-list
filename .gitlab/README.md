# GitLab CI/CD Скрипты и Конфигурация

Этот каталог содержит скрипты и конфигурацию для автоматизации CI/CD процессов в GitLab.

## 📁 Структура

```
.gitlab/
├── README.md                    # Этот файл
├── CLAUDE_CODE_REVIEW.md        # Документация по автоматическому code review
└── scripts/
    ├── claude-review.py         # Скрипт для автоматического code review с Claude AI
    └── requirements.txt         # Python зависимости для скриптов
```

## 🤖 Автоматический Code Review с Claude

Репозиторий настроен для автоматического ревью merge requests с помощью Claude AI.

### Быстрый старт

1. **Настройте переменные окружения** в GitLab (Settings → CI/CD → Variables):
   - `ANTHROPIC_API_KEY` - ваш API ключ от Anthropic
   - `GITLAB_TOKEN` - GitLab Access Token с правами `api`

2. **Создайте Merge Request** - Claude автоматически проведет code review

3. **Получите результаты** - Claude опубликует детальный review как комментарий в MR

### Полная документация

См. [CLAUDE_CODE_REVIEW.md](./CLAUDE_CODE_REVIEW.md) для:
- Детальных инструкций по настройке
- Описания возможностей
- Troubleshooting
- Best practices
- Примеров использования

## 🛠️ Скрипты

### `scripts/claude-review.py`

Автоматический code review с использованием Claude API.

**Что делает:**
- Получает изменения из merge request (git diff)
- Отправляет изменения в Claude API для анализа
- Получает детальный code review
- Публикует результаты как комментарий в MR

**Использование:**
```bash
# Устанавливается автоматически в GitLab CI/CD pipeline
python .gitlab/scripts/claude-review.py
```

**Зависимости:**
```bash
pip install -r .gitlab/scripts/requirements.txt
```

## 🚀 Использование

Скрипты автоматически запускаются в GitLab CI/CD pipeline при создании merge request.

Конфигурация находится в корневом файле `.gitlab-ci.yml`.

## 📝 Логи

Все скрипты выводят подробные логи в stdout, которые можно просмотреть в:
**CI/CD → Pipelines → [выберите pipeline] → [выберите job]**

## 🔐 Безопасность

- Все секреты хранятся в GitLab CI/CD Variables (Settings → CI/CD → Variables)
- Переменные помечены как Protected и Masked
- Скрипты не логируют конфиденциальную информацию

## 💡 Добавление новых скриптов

Для добавления нового скрипта:

1. Создайте файл в `scripts/`
2. Сделайте файл исполняемым (если bash): `chmod +x scripts/your-script.sh`
3. Добавьте зависимости в `requirements.txt` (если Python)
4. Добавьте job в `.gitlab-ci.yml`
5. Задокументируйте использование в этом README

## 📚 Дополнительные ресурсы

- [GitLab CI/CD Documentation](https://docs.gitlab.com/ee/ci/)
- [Anthropic API Documentation](https://docs.anthropic.com/)
- [Python GitLab API](https://python-gitlab.readthedocs.io/)

---

Для вопросов и предложений создавайте issue в репозитории.
