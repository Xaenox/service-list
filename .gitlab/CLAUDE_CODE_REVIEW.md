# 🤖 Автоматический Code Review с Claude для GitLab

Этот репозиторий настроен для автоматического ревью merge requests с помощью Claude AI от Anthropic.

## 📋 Возможности

- ✅ Автоматический анализ всех изменений в merge request
- ✅ Выявление критичных проблем (security, bugs, breaking changes)
- ✅ Проверка соблюдения best practices
- ✅ Конструктивные предложения по улучшению кода
- ✅ Автоматическая публикация результатов как комментарий в MR
- ✅ Поддержка Kotlin/Ktor backend и React/TypeScript frontend

## 🚀 Быстрый старт

### 1. Получение Anthropic API ключа

1. Зарегистрируйтесь на [Anthropic Console](https://console.anthropic.com/)
2. Перейдите в раздел API Keys
3. Создайте новый API ключ
4. Скопируйте ключ (он начинается с `sk-ant-...`)

### 2. Получение GitLab токена

1. В GitLab перейдите: **Settings → Access Tokens** (или **User Settings → Access Tokens**)
2. Создайте новый Project Access Token или Personal Access Token со следующими правами:
   - `api` - полный доступ к API
   - `write_repository` - для публикации комментариев
3. Скопируйте токен

### 3. Настройка CI/CD переменных в GitLab

Перейдите в настройки вашего проекта:
**Settings → CI/CD → Variables** и добавьте следующие переменные:

#### Обязательные переменные:

| Переменная | Значение | Защищенная | Маскированная |
|------------|----------|------------|---------------|
| `ANTHROPIC_API_KEY` | Ваш Anthropic API ключ | ✅ Да | ✅ Да |
| `GITLAB_TOKEN` | Ваш GitLab Access Token | ✅ Да | ✅ Да |

#### Опциональные переменные:

| Переменная | Значение по умолчанию | Описание |
|------------|----------------------|----------|
| `CLAUDE_MODEL` | `claude-sonnet-4-20250514` | Модель Claude для использования |

### 4. Проверка настройки

1. Создайте новую ветку:
   ```bash
   git checkout -b test-claude-review
   ```

2. Внесите какие-либо изменения в код

3. Создайте commit и push:
   ```bash
   git add .
   git commit -m "test: проверка автоматического code review"
   git push origin test-claude-review
   ```

4. Создайте Merge Request в GitLab

5. Дождитесь завершения pipeline (обычно 2-5 минут)

6. Claude автоматически добавит комментарий с code review в ваш MR!

## 📊 Что анализирует Claude

Claude проводит комплексный анализ всех изменений:

### 🔴 Критичные проблемы
- **Security vulnerabilities** - SQL injection, XSS, command injection и т.д.
- **Bugs** - потенциальные ошибки в логике
- **Breaking changes** - изменения, которые могут сломать существующий функционал
- **Data loss risks** - риски потери данных

### 🟡 Важные замечания
- **Code quality** - дублирование кода, сложность, читаемость
- **Best practices** - соблюдение паттернов и стандартов
- **Performance issues** - потенциальные проблемы с производительностью
- **Error handling** - правильная обработка ошибок

### 🔵 Предложения по улучшению
- **Refactoring opportunities** - возможности для рефакторинга
- **Code organization** - структура кода
- **Documentation** - комментарии и документация
- **Test coverage** - покрытие тестами

## 🛠️ Конфигурация

### Изменение модели Claude

Вы можете использовать разные модели Claude в зависимости от ваших потребностей:

| Модель | Описание | Когда использовать |
|--------|----------|-------------------|
| `claude-opus-4-20250514` | Самая мощная модель | Сложные проекты, критичный код |
| `claude-sonnet-4-20250514` | Баланс качества и скорости (по умолчанию) | Обычная разработка |
| `claude-haiku-3-5-20250514` | Быстрая и экономичная | Частые MR, небольшие изменения |

Установите переменную `CLAUDE_MODEL` в Settings → CI/CD → Variables.

### Настройка GitLab CI/CD

Файл `.gitlab-ci.yml` содержит stage `code-review`:

```yaml
claude-review:
  stage: code-review
  image: python:3.11-slim
  before_script:
    - pip install anthropic requests python-gitlab
  script:
    - python .gitlab/scripts/claude-review.py
  allow_failure: true
  rules:
    - if: $CI_PIPELINE_SOURCE == "merge_request_event"
```

#### Параметры:
- `allow_failure: true` - pipeline не упадет, если review не удастся
- `rules` - запускается только для merge requests

### Кастомизация промпта

Вы можете изменить промпт для Claude, отредактировав файл `.gitlab/scripts/claude-review.py`.

Найдите секцию с `prompt = f"""...` и измените инструкции по вашему усмотрению.

## 🎯 Примеры использования

### Пример 1: Стандартный MR

```bash
# Создайте feature branch
git checkout -b feature/add-new-endpoint

# Внесите изменения
# ... код ...

# Commit и push
git add .
git commit -m "feat: add new API endpoint for user settings"
git push origin feature/add-new-endpoint

# Создайте MR в GitLab UI
```

Claude автоматически:
1. Проанализирует все изменения в diff
2. Проверит на security issues
3. Оценит качество кода
4. Даст рекомендации
5. Опубликует результаты в MR

### Пример 2: Множество файлов

Claude эффективно анализирует изменения в любом количестве файлов:
- Kotlin backend код
- React/TypeScript frontend
- SQL миграции
- Конфигурационные файлы
- Docker файлы

### Пример 3: Рефакторинг

При рефакторинге Claude проверит:
- Сохранена ли функциональность
- Улучшилась ли структура кода
- Нет ли регрессий
- Соответствие best practices

## 🔧 Troubleshooting

### Pipeline не запускается для MR

**Проблема**: Claude review не запускается автоматически.

**Решение**:
1. Проверьте, что `.gitlab-ci.yml` закоммичен в репозиторий
2. Убедитесь, что GitLab CI/CD включен: Settings → CI/CD → General pipelines
3. Проверьте правило в `.gitlab-ci.yml`:
   ```yaml
   rules:
     - if: $CI_PIPELINE_SOURCE == "merge_request_event"
   ```

### Ошибка "ANTHROPIC_API_KEY не установлена"

**Проблема**: `❌ Ошибка: переменная окружения ANTHROPIC_API_KEY не установлена`

**Решение**:
1. Убедитесь, что переменная добавлена в Settings → CI/CD → Variables
2. Проверьте, что переменная не защищена только для protected branches
3. Если MR из fork - переменные могут быть недоступны по соображениям безопасности

### Ошибка "GITLAB_TOKEN не установлена"

**Проблема**: Review выполняется, но не публикуется в MR.

**Решение**:
1. Создайте GitLab Access Token с правами `api` и `write_repository`
2. Добавьте токен как переменную `GITLAB_TOKEN` в Settings → CI/CD → Variables
3. Убедитесь, что токен не истек

### Claude API rate limit

**Проблема**: `Rate limit exceeded`

**Решение**:
1. Проверьте лимиты вашего Anthropic аккаунта
2. Рассмотрите использование более легкой модели (haiku вместо opus)
3. Настройте `allow_failure: true` чтобы не блокировать MR

### Review не учитывает все изменения

**Проблема**: Claude видит не все файлы.

**Решение**:
1. В `.gitlab-ci.yml` установите `GIT_DEPTH: 0` для полного клона
2. Проверьте размер diff - очень большие изменения могут обрезаться
3. Рассмотрите разбиение больших MR на несколько меньших

### Комментарий не публикуется в MR

**Проблема**: Review выполняется успешно, но комментарий не появляется.

**Решение**:
1. Проверьте права GitLab токена
2. Убедитесь, что у бота есть доступ к проекту
3. Проверьте логи job в GitLab CI/CD
4. Review будет выведен в логи, даже если публикация не удалась

## 💡 Best Practices

### 1. Используйте описательные commit messages
Claude учитывает контекст из commit messages и описания MR.

### 2. Добавляйте описание в MR
Опишите цель изменений в Description - Claude будет учитывать это при анализе.

### 3. Разбивайте большие изменения
Маленькие, фокусированные MR получают более качественный review.

### 4. Не игнорируйте замечания Claude
Claude часто выявляет неочевидные проблемы - относитесь к ним серьезно.

### 5. Комбинируйте с человеческим review
Автоматический review - отличное дополнение, но не замена человеческого code review.

## 📈 Метрики и мониторинг

### Просмотр истории reviews

Все reviews сохраняются как комментарии в MR:
1. Откройте MR
2. Найдите комментарии от Claude
3. Сравните reviews между итерациями

### Анализ pipeline

В GitLab CI/CD → Pipelines:
- Время выполнения stage `code-review`
- Частота failures
- Логи выполнения

### Оптимизация затрат

Anthropic API стоимость зависит от:
- Выбранной модели
- Размера анализируемого кода
- Количества MR

**Рекомендации**:
- Используйте Sonnet для баланса качества/цены
- Используйте Haiku для частых небольших MR
- Используйте Opus только для критичного кода

## 🔐 Безопасность

### Хранение секретов

- ✅ **Всегда** храните API ключи в GitLab CI/CD Variables
- ✅ **Всегда** помечайте их как Protected и Masked
- ❌ **Никогда** не коммитьте ключи в код
- ❌ **Никогда** не логируйте секреты

### Доступ к коду

- Claude API получает только diff из MR
- Код отправляется через защищенное HTTPS соединение
- Anthropic не использует данные для обучения (согласно политике)

### Fork MR безопасность

Для MR из fork'ов:
- Protected переменные недоступны по умолчанию
- Рассмотрите ручной запуск pipeline для fork MR
- Или настройте отдельный токен только для чтения

## 📚 Дополнительные ресурсы

- [Anthropic API Documentation](https://docs.anthropic.com/)
- [GitLab CI/CD Documentation](https://docs.gitlab.com/ee/ci/)
- [Claude Models Overview](https://docs.anthropic.com/claude/docs/models-overview)

## 🤝 Поддержка

Если возникли проблемы:
1. Проверьте раздел Troubleshooting выше
2. Просмотрите логи pipeline в GitLab
3. Создайте issue в репозитории с описанием проблемы

---

**Версия**: 1.0.0
**Дата**: 2025-12-25
**Модель по умолчанию**: claude-sonnet-4-20250514
