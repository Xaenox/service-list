# 🤖 Автоматический Code Review с Claude для GitHub

Этот репозиторий настроен для автоматического ревью pull requests с помощью Claude AI от Anthropic.

## 📋 Возможности

- ✅ Автоматический анализ всех изменений в pull request
- ✅ Выявление критичных проблем (security, bugs, breaking changes)
- ✅ Проверка соблюдения best practices
- ✅ Конструктивные предложения по улучшению кода
- ✅ Автоматическая публикация результатов как комментарий в PR
- ✅ Обновление комментариев при новых коммитах
- ✅ Поддержка Kotlin/Ktor backend и React/TypeScript frontend

## 🚀 Быстрый старт

### 1. Получение Anthropic API ключа

1. Зарегистрируйтесь на [Anthropic Console](https://console.anthropic.com/)
2. Перейдите в раздел **API Keys**
3. Создайте новый API ключ
4. Скопируйте ключ (он начинается с `sk-ant-...`)

### 2. Настройка GitHub Secrets

Перейдите в настройки вашего репозитория:
**Settings → Secrets and variables → Actions**

#### Добавьте Secret:

1. Нажмите **New repository secret**
2. **Name**: `ANTHROPIC_API_KEY`
3. **Value**: ваш API ключ от Anthropic
4. Нажмите **Add secret**

> **Примечание**: `GITHUB_TOKEN` уже доступен автоматически в GitHub Actions

### 3. Проверка настройки

1. Создайте новую ветку:
   ```bash
   git checkout -b test-claude-review
   ```

2. Внесите какие-либо изменения в код:
   ```bash
   echo "// test comment" >> src/main/kotlin/club/vas3k/services/Application.kt
   git add .
   git commit -m "test: проверка автоматического code review"
   ```

3. Создайте push и Pull Request:
   ```bash
   git push origin test-claude-review
   ```

4. Откройте Pull Request в GitHub UI

5. Дождитесь завершения workflow **Claude AI Code Review** (обычно 1-3 минуты)

6. Claude автоматически добавит детальный комментарий с code review! 🤖

## 📊 Что анализирует Claude

Claude проводит комплексный анализ всех изменений:

### 🔴 Критичные проблемы
- **Security vulnerabilities** - SQL injection, XSS, CSRF, command injection
- **Bugs** - потенциальные ошибки в логике, null pointer exceptions
- **Breaking changes** - изменения API, несовместимость версий
- **Data loss risks** - риски потери данных

### 🟡 Важные замечания
- **Code quality** - дублирование кода, сложность, читаемость
- **Best practices** - соблюдение Kotlin/React/TypeScript конвенций
- **Performance issues** - неэффективные алгоритмы, N+1 queries
- **Error handling** - правильная обработка исключений
- **Resource management** - утечки памяти, незакрытые соединения

### 🔵 Предложения по улучшению
- **Refactoring opportunities** - улучшение структуры кода
- **Code organization** - архитектура и разделение ответственности
- **Documentation** - комментарии и JavaDoc/KDoc
- **Test coverage** - недостающие тесты
- **Type safety** - использование типов вместо Any/unknown

## 🛠️ Конфигурация

### Изменение модели Claude

Вы можете использовать разные модели Claude в зависимости от ваших потребностей:

| Модель | Описание | Когда использовать |
|--------|----------|-------------------|
| `claude-opus-4-20250514` | Самая мощная модель | Сложные проекты, критичный код |
| `claude-sonnet-4-20250514` | Баланс качества и скорости (по умолчанию) | Обычная разработка |
| `claude-haiku-3-5-20250514` | Быстрая и экономичная | Частые PR, небольшие изменения |

**Для изменения модели:**

1. Перейдите: **Settings → Secrets and variables → Actions → Variables**
2. Нажмите **New repository variable**
3. **Name**: `CLAUDE_MODEL`
4. **Value**: `claude-opus-4-20250514` (или другая модель)
5. Нажмите **Add variable**

### Настройка GitHub Actions workflow

Файл `.github/workflows/claude-review.yml` содержит конфигурацию:

```yaml
on:
  pull_request:
    types: [opened, synchronize, reopened]
    branches:
      - main
      - master
      - develop
```

#### Параметры:
- `types` - события PR, при которых запускается review
  - `opened` - новый PR
  - `synchronize` - новые коммиты в PR
  - `reopened` - переоткрытый PR
- `branches` - целевые ветки для review

### Кастомизация промпта

Вы можете изменить промпт для Claude, отредактировав файл `.github/scripts/claude-review.py`.

Найдите секцию с `prompt = f"""...` и измените инструкции по вашему усмотрению.

## 🎯 Примеры использования

### Пример 1: Новая фича

```bash
# Создайте feature branch
git checkout -b feature/add-notifications

# Реализуйте функционал
# ... код ...

# Commit и push
git add .
git commit -m "feat: add email notifications for service updates"
git push origin feature/add-notifications

# Создайте PR в GitHub UI
```

Claude автоматически:
1. Проанализирует все изменения
2. Проверит на security issues
3. Оценит качество кода
4. Даст рекомендации
5. Опубликует результаты в PR

### Пример 2: Bug fix

```bash
git checkout -b fix/oauth-redirect-bug
# ... исправление ...
git add .
git commit -m "fix: correct OAuth redirect URL handling"
git push origin fix/oauth-redirect-bug
```

Claude проверит:
- Исправлена ли реальная причина бага
- Нет ли регрессий
- Достаточно ли error handling
- Нужны ли дополнительные тесты

### Пример 3: Рефакторинг

```bash
git checkout -b refactor/simplify-auth-flow
# ... рефакторинг ...
git add .
git commit -m "refactor: simplify authentication flow"
git push origin refactor/simplify-auth-flow
```

Claude оценит:
- Улучшилась ли читаемость
- Сохранена ли функциональность
- Соответствие best practices
- Упрощение или усложнение

## 🔧 Troubleshooting

### Workflow не запускается для PR

**Проблема**: Claude review не запускается автоматически.

**Решение**:
1. Проверьте, что workflow файл закоммичен: `.github/workflows/claude-review.yml`
2. Убедитесь, что GitHub Actions включены: **Settings → Actions → General**
3. Проверьте целевую ветку - workflow запускается только для main/master/develop
4. Для PR из fork - см. секцию "Fork PR безопасность" ниже

### Ошибка "ANTHROPIC_API_KEY not set"

**Проблема**: `❌ Ошибка: переменная окружения ANTHROPIC_API_KEY не установлена`

**Решение**:
1. Убедитесь, что Secret добавлен: **Settings → Secrets and variables → Actions**
2. Проверьте правильность имени: должно быть точно `ANTHROPIC_API_KEY`
3. Пересоздайте Secret, если необходимо

### Комментарий не появляется в PR

**Проблема**: Workflow выполняется успешно, но комментария нет.

**Решение**:
1. Проверьте права workflow: **Settings → Actions → General → Workflow permissions**
   - Должно быть: "Read and write permissions"
2. Убедитесь, что `GITHUB_TOKEN` имеет права на pull-requests
3. Проверьте логи workflow - review может быть выведен в логи
4. Для приватных репозиториев убедитесь, что у токена есть необходимые права

### Rate limit exceeded

**Проблема**: `Rate limit exceeded` от Anthropic API

**Решение**:
1. Проверьте лимиты вашего Anthropic аккаунта в [console.anthropic.com](https://console.anthropic.com/)
2. Рассмотрите использование более легкой модели (Haiku вместо Opus)
3. Уменьшите частоту PR
4. Свяжитесь с Anthropic для увеличения лимитов

### Review обрезается

**Проблема**: Очень большие изменения анализируются не полностью.

**Решение**:
1. Diff обрезается до 15000 символов для оптимизации
2. Разбивайте большие PR на несколько меньших
3. Фокусируйтесь на атомарных изменениях
4. Можно увеличить лимит в `claude-review.py` (строка `{diff[:15000]}`)

## 💡 Best Practices

### 1. Используйте описательные PR descriptions
Claude учитывает описание PR при анализе. Четко опишите:
- Что изменено
- Почему изменено
- Как это работает

### 2. Делайте небольшие, фокусированные PR
- Маленькие PR получают более качественный review
- Легче найти проблемы
- Быстрее мержить

### 3. Не игнорируйте замечания Claude
Claude часто выявляет неочевидные проблемы:
- Security уязвимости
- Потенциальные баги
- Антипаттерны

### 4. Комбинируйте с человеческим review
- Автоматический review - дополнение, не замена
- Человек лучше понимает бизнес-логику
- Claude лучше находит технические проблемы

### 5. Используйте правильную модель
- **Opus** - для критичного кода (auth, payments, security)
- **Sonnet** - для обычной разработки (рекомендуется)
- **Haiku** - для простых изменений (docs, configs)

### 6. Добавляйте контекст в коммиты
Хорошие commit messages помогают Claude лучше понять изменения:

```bash
# ✅ Хорошо
git commit -m "feat: add rate limiting to API endpoints to prevent abuse"

# ❌ Плохо
git commit -m "fix stuff"
```

## 🔐 Безопасность

### Хранение секретов

- ✅ **Всегда** храните API ключи в GitHub Secrets
- ✅ **Никогда** не коммитьте ключи в код
- ✅ **Никогда** не логируйте секреты
- ✅ Регулярно ротируйте API ключи

### Доступ к коду

- Claude API получает только diff из PR
- Код отправляется через защищенное HTTPS соединение
- Anthropic не использует данные для обучения моделей (согласно политике)

### Fork PR безопасность

Для PR из fork'ов:
- Secrets недоступны по умолчанию (защита от утечки)
- Workflow требует manual approval от мейнтейнера
- Настройка: **Settings → Actions → General → Fork pull request workflows**
  - Рекомендуется: "Require approval for first-time contributors"

## 📈 Метрики и мониторинг

### Просмотр истории reviews

Все reviews сохраняются как комментарии в PR:
1. Откройте PR
2. Найдите комментарии от GitHub Actions bot
3. При новых коммитах комментарий обновляется

### Анализ workflow

В **Actions** табе:
- История всех запусков
- Время выполнения review
- Частота failures
- Логи выполнения

### Оптимизация затрат

Стоимость зависит от:
- Выбранной модели Claude
- Размера анализируемого кода
- Количества PR

**Приблизительная стоимость** (на январь 2025):
- **Sonnet**: ~$0.01-0.05 за обычный PR
- **Opus**: ~$0.05-0.20 за обычный PR
- **Haiku**: ~$0.001-0.01 за обычный PR

## 🔄 Обновление

Для обновления на новую версию:

1. **Pull последние изменения**:
   ```bash
   git pull origin main
   ```

2. **Обновите зависимости** (если изменились):
   - Проверьте `.github/scripts/requirements.txt`

3. **Проверьте новые настройки**:
   - Просмотрите изменения в workflow файле
   - Обновите Secrets/Variables если нужно

## 📚 Дополнительные ресурсы

- [Anthropic API Documentation](https://docs.anthropic.com/)
- [GitHub Actions Documentation](https://docs.github.com/actions)
- [Claude Models Overview](https://docs.anthropic.com/claude/docs/models-overview)
- [PyGithub Documentation](https://pygithub.readthedocs.io/)

## 🤝 Поддержка

Если возникли проблемы:
1. Проверьте раздел **Troubleshooting** выше
2. Просмотрите логи workflow в **Actions** табе
3. Создайте issue в репозитории с:
   - Описанием проблемы
   - Логами workflow (без секретов!)
   - Версией Python и зависимостей

## 📝 Примеры review комментариев

### Пример 1: Все хорошо
```markdown
## 🤖 Автоматический Code Review от Claude

**Общая оценка**: ✅ Отличные изменения! Код качественный и следует best practices.

**Положительные моменты:**
- Чистая реализация с хорошим разделением ответственности
- Добавлена корректная обработка ошибок
- Код хорошо типизирован

**Вывод:** ✅ Можно мержить
```

### Пример 2: С замечаниями
```markdown
## 🤖 Автоматический Code Review от Claude

**Общая оценка**: ⚠️ Хорошие изменения, но есть несколько важных замечаний.

**Проблемы и замечания:**

🔴 **Критичные проблемы:**
- SQL запрос в строке 45 уязвим к SQL injection - используйте параметризованные запросы

🟡 **Важные замечания:**
- Отсутствует обработка ошибок при вызове внешнего API (строка 78)
- Функция `processData` слишком большая - рассмотрите разбиение на меньшие функции

**Вывод:** ⚠️ Рекомендуется исправить замечания перед мержем
```

---

**Версия**: 1.0.0
**Дата**: 2025-12-25
**Модель по умолчанию**: claude-sonnet-4-20250514
